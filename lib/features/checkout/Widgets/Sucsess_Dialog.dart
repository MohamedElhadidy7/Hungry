import 'package:flutter/cupertino.dart';

class SucsessDialog extends StatelessWidget {
  const SucsessDialog({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
